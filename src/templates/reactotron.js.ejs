import Reactotron from 'reactotron-react-<%
if(props.reactNative) {
  %>native<%
} else {
  %>js<%
}%>'

if (__DEV__) {
  const tron = Reactotron.configure()
    <% if(props.reactNative) {%>
      .useReactNative()
    <%}%>
    .connect()
  console.tron = tron

  tron.clear()
}
